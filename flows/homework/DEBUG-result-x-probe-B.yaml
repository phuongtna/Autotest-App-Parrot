# DEBUG PROBE B (tạm, xoá sau khi xong) - chạy NGAY SAU probe A (app đang đứng nguyên tại
# exercise_result_screen, KHÔNG launchApp lại để không mất trạng thái đó). Chỉ làm đúng 1 việc:
# tap X bằng cách hiện tại repo đang dùng (point 92%,6%) rồi CHỤP LẠI trạng thái ngay sau đó, không
# thêm bất kỳ workaround nào (không tăng timeout ngoài mức cần để đọc trạng thái, không bấm nút
# khác, không back).
appId: ${APP_ID}
---
- takeScreenshot: DEBUG-probeB-before-tap
- tapOn:
    point: "92%,6%"
- takeScreenshot: DEBUG-probeB-immediately-after-tap
